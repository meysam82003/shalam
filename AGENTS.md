# دیوان امتیاز — دستور کار Codex / Work

## هدف پروژه
این ریپو نسخهٔ Android بومی «دیوان امتیاز» است؛ ابزار داوری و ثبت امتیاز برای سه بازی:
- شلم
- منفی / حکم ثابت
- هزارتایی

خود بازی ورق داخل اپ اجرا نمی‌شود. اپ نقش داور، دفتر امتیاز، مدیر گروه‌ها/بازیکنان، تاریخچه و تنظیمات را دارد.

## قواعد غیرقابل تغییر
1. Native Android باقی بماند؛ WebView/PWA/Cordova/Ionic اضافه نشود.
2. Android 5.0+ و عملکرد اصلی کاملاً آفلاین حفظ شود.
3. هیچ قابلیت فعلی برای اجرای UI جدید حذف، محدود یا بازطراحی مخرب نشود؛ تغییرات افزایشی باشند.
4. نام نمایشی بازی سوم در تمام UI، تاریخچه، تنظیمات، اعلان‌ها و خروجی‌ها دقیقاً «هزارتایی» است.
5. RTL واقعی، فونت فارسی، ذخیرهٔ خودکار، ادامهٔ بازی، ویرایش/حذف دست و تاریخچه حفظ شوند.
6. منطق فعلی شلم، منفی و هزارتایی بدون دلیل فنی تغییر نکند؛ در صورت تغییر، تست پوشش داده شود.
7. قبل از تحویل، Unit Test و Release Build باید پاس شوند.

## مرجع قطعی UI/UX
هویت بصری مورد تأیید کاربر:
- پس‌زمینهٔ سرمه‌ای/لاجوردی تیره
- طلایی/برنزی برای قاب‌ها، تاج و CTA اصلی
- فیروزه‌ای برای موفقیت/انتخاب
- زرشکی برای هشدار و امتیاز منفی
- آواتارها و المان‌ها به سبک سه‌بعدی انیمیشنی ایرانی
- الهام از پاسارگاد/هخامنشی + تزئینات قاجاری
- کارت‌های برجسته، گوشه‌های گرد، قاب‌های طلایی، نور و سایهٔ نرم
- ظاهر پیش‌فرض Material هدف نیست؛ رابط باید اختصاصی و نزدیک به کانسپت سلطنتی باشد.

## اجزای بصری که باید یکپارچه باشند
- Splash و کاور برنامه
- Home
- انتخاب بازی
- مدیریت تیم‌ها و آواتارها
- اتاق/آماده‌سازی بازی
- داوری زندهٔ شلم
- داوری زندهٔ منفی با نتایج پنهان
- داوری و رتبه‌بندی هزارتایی
- نتیجه و تاریخچه
- تنظیمات
- نشان‌های امتیاز، آیکون‌ها، کارت‌ها و پس‌زمینه‌ها

## روش کار
- ابتدا ساختار و قابلیت‌های موجود را کامل بررسی کن؛ کد موجود را کورکورانه جایگزین نکن.
- UI موجود را مرحله‌ای به Design System سلطنتی نزدیک کن و در هر مرحله build/test بگیر.
- از assetهای سه‌بعدی موجود در `app/src/main/res/drawable-nodpi/` استفاده کن و فقط در صورت نیاز asset جدید اضافه کن.
- برای تغییر دیتابیس migration امن بنویس و دادهٔ کاربر را حذف نکن.
- نسخهٔ جدید باید روی نسخهٔ قبلی Update شود و نصب مجدد اجباری نباشد.

## Definition of Done
1. `gradle --no-daemon testReleaseUnitTest assembleRelease` پاس شود.
2. APK signed ساخته شود.
3. مسیرهای اصلی برنامه smoke-test شوند.
4. هیچ regression در تاریخچه، ویرایش، ذخیرهٔ خودکار و قوانین امتیازدهی نباشد.
5. UI همهٔ صفحات از یک Design System مشترک استفاده کند و از نظر رنگ، قاب، typography و assetها با کانسپت سلطنتی هم‌خوان باشد.
